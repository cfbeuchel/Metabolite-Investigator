# Interaction Feature
# --> check the #TODO# tag in the scripts

* Multiple testing during interaction based on user-chosen method
* what missing rate to accept during multivariable association
* add UI component (only plotting in new tab)
* add server component
* add output to results
